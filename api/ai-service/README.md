# AI detection service

Install dependencies:

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

The API exposes `/health` and `/detect` on port 5001.
